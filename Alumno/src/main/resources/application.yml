server:
  port: 8082

spring:
  application:
    name: ms-alumnos

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    lease-renewal-interval-in-seconds: 10