server:
  port: 8081

spring:
  application:
    name: ms-cursos

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    lease-renewal-interval-in-seconds: 10
